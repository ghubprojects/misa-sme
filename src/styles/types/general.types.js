export const ColorScheme = Object.freeze({
    Primary: 'primary',
    Red: 'red'
});

export const TooltipDirection = Object.freeze({
    Down: 'down',
    Up: 'up',
    Left: 'left',
    Right: 'right'
});
